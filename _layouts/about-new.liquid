---
layout: default
---

<!-- 引入学术风格衬线字体 -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Crimson+Pro:ital,wght@0,400;0,500;0,600;0,700;1,400;1,500&family=Playfair+Display:ital,wght@0,500;0,600;0,700;1,500&family=Source+Serif+4:ital,opsz,wght@0,8..60,400;0,8..60,500;0,8..60,600;0,8..60,700;1,8..60,400;1,8..60,500&display=swap" rel="stylesheet">

<div class="post">
  <header class="post-header">
    <h1 class="post-title">
      {% if site.title == 'blank' %}
        <strong><span>{{ site.first_name }}</span></strong>
        <span>{{ site.last_name }}</span>
        <span>({{ site.Chinese_name }})</span>
      {% else %}
        {{ site.title }}
      {% endif %}
    </h1>

    <!-- 社交图标 -->
    <div class="social-icons-header">
      {% if site.email %}
        <a href="mailto:{{ site.email }}" title="Email">
          <i class="fas fa-envelope"></i>
        </a>
      {% endif %}
      {% if site.github_username %}
        <a href="https://github.com/{{ site.github_username }}" title="GitHub" target="_blank">
          <i class="fab fa-github"></i>
        </a>
      {% endif %}
      {% if site.scholar_userid %}
        <a href="https://scholar.google.com/citations?user={{ site.scholar_userid }}" title="Google Scholar" target="_blank">
          <i class="ai ai-google-scholar"></i>
        </a>
      {% endif %}
      {% if site.orcid_id %}
        <a href="https://orcid.org/{{ site.orcid_id }}" title="ORCID" target="_blank">
          <i class="fab fa-orcid"></i>
        </a>
      {% endif %}
      {% if site.linkedin_username %}
        <a href="https://www.linkedin.com/in/{{ site.linkedin_username }}" title="LinkedIn" target="_blank">
          <i class="fab fa-linkedin"></i>
        </a>
      {% endif %}
      {% if site.twitter_username %}
        <a href="https://twitter.com/{{ site.twitter_username }}" title="Twitter" target="_blank">
          <i class="fab fa-twitter"></i>
        </a>
      {% endif %}
    </div>
  </header>
</div>

<article>
  {% if page.profile %}
    <div class="profile float-{% if page.profile.align == 'left' %}left{% else %}right{% endif %}">
      {% if page.profile.image %}
        {% assign profile_image_path = page.profile.image | prepend: 'assets/img/' %}
        {% if page.profile.image_circular %}
          {% assign profile_image_class = 'img-fluid z-depth-1 rounded-circle' %}
        {% else %}
          {% assign profile_image_class = 'img-fluid z-depth-1 rounded' %}
        {% endif %}
        {% capture sizes %}(min-width: {{ site.max_width }}) {{ site.max_width | minus: 30 | times: 0.3}}px, (min-width: 576px) 30vw, 95vw"{% endcapture %}
        {% include figure.liquid loading="eager" path=profile_image_path class=profile_image_class sizes=sizes alt=page.profile.image cache_bust=true %}
      {% endif %}
      {% if page.profile.more_info %}
        <div class="more-info">{{ page.profile.more_info }}</div>
      {% endif %}
    </div>
  {% endif %}

  <div class="clearfix">{{ content }}</div>

  <!-- News -->
  {% if page.news and site.announcements.enabled %}
    <h2 class="section-title">
      <span>👀 News</span>
    </h2>
    {% include news.liquid %}
  {% endif %}

  <!-- Education Experience -->
  {% include about-education.liquid %}

  <!-- Research Experience -->
  {% include about-research.liquid %}

  <!-- Publications -->
  {% if page.publications and site.publications.enabled %}
    <h2 class="section-title">
      <span>📚 Publications</span>
    </h2>
    <div class="publications">
      {% bibliography --cited %}
    </div>
  {% endif %}

  <!-- Honors & Awards -->
  {% if page.honors %}
    <h2 class="section-title">
      <span>🏆 Honors & Awards</span>
    </h2>
    {% include about-honors.liquid %}
  {% endif %}

  <!-- Teaching Experience -->
  {% if page.teaching %}
    <h2 class="section-title">
      <span>👨‍🏫 Teaching Experience</span>
    </h2>
    {% include about-teaching.liquid %}
  {% endif %}

  <!-- Hobbies -->
  {% if page.hobbies %}
    <h2 class="section-title">
      <span>🎯 Hobbies</span>
    </h2>
    {% include about-hobbies.liquid %}
  {% endif %}

  <!-- Contact -->
  {% if page.contact %}
    <h2 class="section-title">
      <span>📮 Contact</span>
    </h2>
    {% include about-contact.liquid %}
  {% endif %}
</article>

<!-- Load all stylesheets from _sass/about-*.scss -->
<!-- These are automatically compiled into main.scss via @import statements -->
